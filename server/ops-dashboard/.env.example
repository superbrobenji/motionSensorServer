VITE_API_URL=http://localhost:8080
VITE_SIDECAR_URL=http://localhost:9000
# ADMIN_KEY is NOT embedded here — sidecar auth uses server-side header injection
# The ops dashboard sends ADMIN_KEY from sessionStorage (entered at login screen)
